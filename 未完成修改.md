stm32assistant

先确认项目环境：当前目标是 **STM32F429xx + FreeRTOS**。这次修改不涉及 LVGL API，但我仍会核对 LVGL 版本，并以当前
`communicate_task.c` 的实际实现为准给出可直接落地的修改，不用泛化伪代码。

可以。先纠正一个重要判断：你当前的 `communicate_task.c` 已经在 `App/Task/Src/communicate_task.c:91` 使用
`while (xQueueReceive(...))` 排空队列，所以“每次只取一个请求”的问题已经不存在。

**现在的问题反而是排空得过头了：第一个写请求还没收到 ACK，循环就可能继续发送第二个写请求，并在
`App/Task/Src/communicate_task.c:111` 覆盖 `pending_request`。**

当前项目环境是：

- MCU：STM32F429xx
- RTOS：FreeRTOS
- LVGL：9.5.0，本次改动不涉及 LVGL API

建议不要继续在现有 `switch` 上零散打补丁，而是把 CommunicateTask 明确拆成四部分：

1. 接收并解析串口帧；
2. 检查 pending 写请求是否超时；
3. 把完成响应可靠投递给 TestTask；
4. 从请求队列中启动下一项事务。

---

# 一、先确定事务规则

建议明确以下规则：

```text
request_id == 0：不需要向业务任务回传结果
request_id != 0：需要通过 fpga_response_t 回传结果

读寄存器请求：当前使用 request_id == 0
写测试参数请求：必须使用 request_id != 0

同一时间最多只能存在一个等待 FPGA ACK 的写请求
写请求结束前，暂不发送队列中的后续请求
```

这里我建议先采用 **严格串行**：

```text
发送写参数
→ 等待成功/失败/超时
→ 把结果交给 TestTask
→ 再处理下一条队列请求
```

严格串行比“一边等待写 ACK、一边发送温度读取”保守，但更容易保证响应顺序。写参数确认通常很快，短暂延迟一次温度读取没有实质影响。

---

# 二、修改 `fpga_comm.h`

先定义无业务响应 ID：

```c
#define FPGA_REQUEST_ID_NONE 0U
```

建议把现在过长的字段注释拆开：

```c
typedef struct
{
    /*
     * MCU内部请求编号，不发送给FPGA。
     * FPGA_REQUEST_ID_NONE表示该请求不需要业务响应。
     */
    uint32_t request_id;

    fpga_operation_enum operation; /* 本次通讯要执行的操作 */

    union
    {
        read_instruction_t read_instruction; /* 读取寄存器参数 */
        test_params_t test_params; /* 写入FPGA的测试参数 */
    } request_data;
} fpga_request_t;
```

对应位置：`App/Communication/Inc/fpga_comm.h:30`

---

# 三、改造 `communicate_task.c` 的状态变量

在文件顶部增加超时配置和“等待投递响应”。

```c
#include "FreeRTOS.h"
#include "task.h"
#include "queue.h"
#include "bsp_uart_device.h"
#include "main.h"
#include "fpga_comm.h"
#include "test_task.h"

// 当前DMA单次接收缓冲区为128Byte，完整包输出缓冲区保持相同容量
#define FPGA_COMMAND_BUFFER_SIZE                  128U

// 写参数后等待FPGA应答的最长时间
#define FPGA_WRITE_RESPONSE_TIMEOUT_MS            1000U

// TestTask事件队列已满时，重试投递响应的间隔
#define FPGA_RESPONSE_REPORT_RETRY_MS             10U

extern UART_Device fpga_device;

// 当前等待FPGA应答的请求
static fpga_request_t pending_request;

// 当前是否存在已经发送、但还未收到应答的写请求
static bool pending_request_valid = false;

// 当前pending请求的发送完成时刻
static TickType_t pending_request_start_tick = 0U;

// 等待投递给TestTask的响应
static fpga_response_t pending_response;

// 当前是否有响应尚未成功投递给TestTask
static bool pending_response_valid = false;
```

这里为什么需要两个状态：

```text
pending_request_valid
    表示正在等待FPGA应答

pending_response_valid
    表示FPGA事务已经结束，但结果还没有成功交给TestTask
```

收到 FPGA ACK 后不能继续保持 `pending_request_valid`，因为已经没有必要再等 FPGA。但如果 TestTask 队列已满，也不能把结果直接丢掉，因此需要单独保存
`pending_response`。

---

# 四、增加“保存业务响应”函数

```c
/**
 * @brief 保存一条等待投递给TestTask的通讯响应
 */
static void communicate_prepare_response(
    const fpga_request_t *request,
    fpga_response_status_enum response_status)
{
    if (request == NULL)
    {
        return;
    }

    pending_response.request_id = request->request_id;
    pending_response.operation = request->operation;
    pending_response.response_status = response_status;
    pending_response_valid = true;
}
```

这个函数只负责保存结果，不直接假设 TestTask 队列一定有空间。

---

# 五、增加“尝试投递响应”函数

```c
/**
 * @brief 尝试把通讯结果投递给TestTask
 */
static void communicate_try_report_response(void)
{
    if (!pending_response_valid)
    {
        return;
    }

    if (test_report_fpga_response(&pending_response))
    {
        pending_response_valid = false;
    }
}
```

这样，如果 `test_event_queue` 已满：

```c
test_report_fpga_response()
```

会返回 `false`，响应仍然保留在 `pending_response` 中，CommunicateTask 下一轮继续投递，不会丢失。

---

# 六、增加超时检查

```c
/**
 * @brief 检查当前写请求是否等待应答超时
 */
static void communicate_check_pending_timeout(void)
{
    if (!pending_request_valid)
    {
        return;
    }

    const TickType_t timeout_ticks =
        pdMS_TO_TICKS(FPGA_WRITE_RESPONSE_TIMEOUT_MS);

    const TickType_t elapsed_ticks =
        xTaskGetTickCount() - pending_request_start_tick;

    if (elapsed_ticks < timeout_ticks)
    {
        return;
    }

    communicate_prepare_response(
        &pending_request,
        FPGA_RESPONSE_TIMEOUT);

    pending_request_valid = false;
}
```

这里使用：

```c
xTaskGetTickCount() - pending_request_start_tick
```

而不是直接比较绝对时间，能够适应 FreeRTOS tick 计数器回绕。

注意：

```c
FPGA_WRITE_RESPONSE_TIMEOUT_MS
```

不是 UART DMA 发送超时，而是：

> 命令已经发送完成后，等待 FPGA 业务 ACK 的超时时间。

`1000ms` 是保守初始值，最终应根据 FPGA 最坏响应时间确定。

---

# 七、让任务等待时间适配 pending 超时

现在代码使用：

```c
xTaskNotifyWait(..., portMAX_DELAY);
```

这样如果 FPGA 不返回任何数据，CommunicateTask 会永远睡眠，超时检查没有机会运行。

增加一个等待时间计算函数：

```c
/**
 * @brief 计算CommunicateTask下一次需要被唤醒的时间
 */
static TickType_t communicate_get_wait_ticks(void)
{
    if (pending_response_valid)
    {
        return pdMS_TO_TICKS(FPGA_RESPONSE_REPORT_RETRY_MS);
    }

    if (!pending_request_valid)
    {
        return portMAX_DELAY;
    }

    const TickType_t timeout_ticks =
        pdMS_TO_TICKS(FPGA_WRITE_RESPONSE_TIMEOUT_MS);

    const TickType_t elapsed_ticks =
        xTaskGetTickCount() - pending_request_start_tick;

    if (elapsed_ticks >= timeout_ticks)
    {
        return 0U;
    }

    return timeout_ticks - elapsed_ticks;
}
```

这样：

- 没有 pending：一直睡眠；
- 正在等 ACK：最多睡到 ACK 超时；
- 有响应未投递成功：每 10ms 重试一次；
- RX/TX 通知仍然可以提前唤醒任务。

---

# 八、把串口接收处理拆成函数

```c
/**
 * @brief 处理FPGA串口接收到的全部完整帧
 */
static void communicate_process_received_frames(
    uint8_t *command_buffer,
    uint16_t buffer_size)
{
    uint16_t command_length;

    while ((command_length = fpga_device.get_command(
                &fpga_device,
                command_buffer,
                buffer_size)) > 0U)
    {
        bool write_success = false;

        /*
         * 只有当前确实在等待写参数应答时，
         * 才允许把写结果帧关联到pending请求。
         */
        if (pending_request_valid &&
            pending_request.operation == FPGA_OPERATION_WRITE_TEST_PARAMS &&
            fpga_comm_parse_write_response(
                command_buffer,
                command_length,
                &write_success))
        {
            communicate_prepare_response(
                &pending_request,
                write_success
                    ? FPGA_RESPONSE_SUCCESS
                    : FPGA_RESPONSE_FAIL);

            pending_request_valid = false;
            continue;
        }

        // 普通寄存器响应交给业务解析器处理
        fpga_comm_parse_command(command_buffer, command_length);
    }
}
```

这里和当前代码相比，关键改变是：

```c
communicate_prepare_response(...);
pending_request_valid = false;
```

不再直接：

```c
test_report_fpga_response(&response);
pending_request_valid = false;
```

因为直接投递可能失败。

---

# 九、重写请求队列处理逻辑

这是最重要的一部分。

```c
/**
 * @brief 处理FPGA请求队列
 *
 * 写请求发送成功后立即停止排空队列，等待该事务结束。
 */
static void communicate_process_request_queue(void)
{
    fpga_request_t request;

    /*
     * 正在等待FPGA应答，或者上一个结果还没交给TestTask时，
     * 都不能启动下一项事务。
     */
    if (pending_request_valid || pending_response_valid)
    {
        return;
    }

    while (xQueueReceive(
               fpga_request_queue,
               &request,
               0U) == pdPASS)
    {
        switch (request.operation)
        {
            case FPGA_OPERATION_READ_REGISTERS:
            {
                /*
                 * 当前读请求是无需业务响应的轮询请求。
                 * 发送失败暂时不向TestTask报告。
                 */
                (void) fpga_comm_send_read_command(
                    &request.request_data.read_instruction);
                break;
            }

            case FPGA_OPERATION_WRITE_TEST_PARAMS:
            {
                if (!fpga_comm_send_test_params(
                        &request.request_data.test_params))
                {
                    communicate_prepare_response(
                        &request,
                        FPGA_RESPONSE_SEND_FAILED);
                    return;
                }

                pending_request = request;
                pending_request_start_tick = xTaskGetTickCount();
                pending_request_valid = true;

                /*
                 * 已经启动一个需要等待应答的写事务，
                 * 后续请求继续留在队列中。
                 */
                return;
            }

            default:
                break;
        }
    }
}
```

这段逻辑解决了当前最大的错误：

当前实现是：

```c
while (xQueueReceive(...))
{
    发送写请求A;
    pending_request = A;

    发送写请求B;
    pending_request = B;
}
```

修改后是：

```c
发送写请求A;
pending_request = A;
return;
```

请求 B 仍留在队列中，等 A 完成后再处理。

## 为什么不用 `xQueuePeek`

这里不需要 `xQueuePeek`。只要写请求发送成功后立即 `return`，后续元素自然还留在 FreeRTOS 队列中。

---

# 十、重写主循环

主循环改成以下结构：

```c
void start_communicate_task(void *argument)
{
    uint8_t command_buffer[FPGA_COMMAND_BUFFER_SIZE] = {0};
    uint32_t event_data;
    TickType_t wait_ticks;

    (void) argument;

    fpga_device.bind_rx_task(
        &fpga_device,
        xTaskGetCurrentTaskHandle());

    while (fpga_device.init(&fpga_device) != 0)
    {
        vTaskDelay(pdMS_TO_TICKS(100U));
    }

    for (;;)
    {
        event_data = 0U;
        wait_ticks = communicate_get_wait_ticks();

        /*
         * 有通知时提前唤醒；
         * 没有通知时，在pending事务超时或响应重试时唤醒。
         */
        (void) xTaskNotifyWait(
            0U,
            0xFFFFFFFFU,
            &event_data,
            wait_ticks);

        /*
         * 必须先处理RX，再启动新的写请求。
         * 这样同一轮同时出现RX和TX事件时，旧ACK不会被关联到新请求。
         */
        if ((event_data & FPGA_RX_EVENT) != 0U)
        {
            communicate_process_received_frames(
                command_buffer,
                sizeof(command_buffer));
        }

        communicate_check_pending_timeout();

        communicate_try_report_response();

        /*
         * 无论本轮是不是TX通知，都检查一次队列。
         * 这样旧事务结束后，不需要依赖额外通知才能启动队列中的下一项请求。
         */
        communicate_process_request_queue();
    }
}
```

## 这里为什么无条件检查队列

假设队列里有：

```text
写请求 A
写请求 B
```

收到一次 TX 通知：

1. 发送 A；
2. B 留在队列；
3. A 的 ACK 到达；
4. pending 结束；
5. A 的结果成功交给 TestTask；
6. 同一轮直接检查队列并发送 B。

如果仍然写成：

```c
if (event_data & FPGA_TX_EVENT)
{
    communicate_process_request_queue();
}
```

那么 A 结束后不一定还有新的 TX 通知，B 可能继续滞留。

---

# 十一、修改 `communicate_submit_request()`

当前函数的注释说：

> 返回 `true` 表示请求已经进入通讯队列。

但实现返回的是：

```c
xTaskNotify(...) == pdPASS
```

这两个语义不完全一致。

请求一旦成功进入队列，就不能因为通知结果而返回 `false`，否则调用方会认为提交失败，但请求实际上仍然可能被执行。

建议改成：

```c
bool communicate_submit_request(const fpga_request_t *request)
{
    if (request == NULL ||
        fpga_request_queue == NULL ||
        communicate_taskHandle == NULL)
    {
        return false;
    }

    /*
     * 当前读请求不返回业务响应；
     * 写参数请求必须具有有效的内部请求编号。
     */
    switch (request->operation)
    {
        case FPGA_OPERATION_READ_REGISTERS:
            if (request->request_id != FPGA_REQUEST_ID_NONE)
            {
                return false;
            }
            break;

        case FPGA_OPERATION_WRITE_TEST_PARAMS:
            if (request->request_id == FPGA_REQUEST_ID_NONE)
            {
                return false;
            }
            break;

        default:
            return false;
    }

    if (xQueueSend(
            fpga_request_queue,
            request,
            0U) != pdPASS)
    {
        return false;
    }

    /*
     * 队列才是真正的数据载体，任务通知只是门铃。
     * eSetBits模式下，有效任务句柄通常都会返回pdPASS。
     */
    (void) xTaskNotify(
        communicate_taskHandle,
        FPGA_TX_EVENT,
        eSetBits);

    return true;
}
```

这样返回值与头文件文档一致：

```text
true：已经成功入队
false：没有入队
```

---

# 十二、`test_task.c` 还需要配套修改

只修改 CommunicateTask 还不够。TestTask 接受匹配响应后，应结束自己的 pending 生命周期。

修改响应处理：

```c
case TEST_EVENT_FPGA_RESPONSE:
{
    const fpga_response_t *fpga_response =
        &event.event_data.fpga_response;

    if (test_context.test_state != TEST_STATE_WAIT_PARAM_RESPONSE ||
        fpga_response->request_id !=
            test_context.pending_fpga_request_id ||
        fpga_response->operation !=
            FPGA_OPERATION_WRITE_TEST_PARAMS)
    {
        break;
    }

    // 当前等待的FPGA事务已经结束
    test_context.pending_fpga_request_id =
        FPGA_REQUEST_ID_NONE;

    if (fpga_response->response_status ==
        FPGA_RESPONSE_SUCCESS)
    {
        test_context.test_state =
            TEST_STATE_PARAM_CONFIRM;
    }
    else
    {
        test_context.test_state =
            TEST_STATE_START_FAIL;
    }

    break;
}
```

这里需要补两个地方：

1. 清除 `pending_fpga_request_id`；
2. 在 case 末尾显式 `break`。

初始化也建议明确写出：

```c
static test_context_t test_context = {
    .test_state = TEST_STATE_IDLE,
    .test_request = {},
    .pending_fpga_request_id = FPGA_REQUEST_ID_NONE
};
```

---

# 十三、让请求 ID 跳过 0

当前：

```c
.request_id = next_fpga_request_id++
```

最终会在 `uint32_t` 回绕时得到 `0`。虽然很难实际发生，但既然已经规定 0 是特殊值，就应该从设计上跳过。

增加：

```c
/**
 * @brief 分配一个非零FPGA请求编号
 */
static uint32_t test_allocate_fpga_request_id(void)
{
    const uint32_t request_id = next_fpga_request_id++;

    if (next_fpga_request_id == FPGA_REQUEST_ID_NONE)
    {
        next_fpga_request_id = 1U;
    }

    return request_id;
}
```

构造写请求时改成：

```c
.request_id = test_allocate_fpga_request_id(),
```

温度读取保持：

```c
.request_id = FPGA_REQUEST_ID_NONE,
```

---

# 十四、不要现在加入自动重试

我的明确建议是： **先只做超时失败，不要自动重发写参数。**

原因是当前 `request_id` 不发送给 FPGA。发生：

```text
请求 A 超时
自动重发请求 A
旧请求 A 的迟到 ACK 到达
```

MCU 无法判断这个 ACK 对应第一次发送还是重试。

写参数虽然看起来是幂等操作，但迟到 ACK 仍可能让 MCU 在第二次实际失败时误判成功。

正确优先级应该是：

1. 先串行化；
2. 先实现超时并报告失败；
3. 验证 FPGA 最坏响应时间；
4. 如果确实需要自动重试，最好扩展协议，让 FPGA 回显事务号；
5. 如果不能改 FPGA 协议，再单独设计迟到 ACK 隔离策略。

不能因为枚举里有 `TIMEOUT` 就顺手加“超时立即重发”。

---

# 十五、`FORMAT_ERROR` 暂时不要在 CommunicateTask 里硬凑

目前：

```c
fpga_comm_parse_write_response()
```

返回 `false` 同时可能表示：

- 这根本不是写响应；
- 长度不匹配；
- 地址不匹配；
- 功能码不匹配。

CommunicateTask 无法区分“普通读取响应”和“损坏的写响应”。

因此暂时不要把所有 `false` 都映射为：

```c
FPGA_RESPONSE_FORMAT_ERROR
```

否则正常温度响应也会被误判成写响应格式错误。

如果后续要实现 `FORMAT_ERROR`，应把解析器改成三态结果，例如：

```c
typedef enum
{
    FPGA_WRITE_RESPONSE_NOT_MATCHED = 0,
    FPGA_WRITE_RESPONSE_VALID,
    FPGA_WRITE_RESPONSE_INVALID
} fpga_write_response_parse_result_enum;
```

这是下一步，不需要和本次事务生命周期修复混在一起。

---

# 修改后的状态关系

最终 CommunicateTask 内部应当满足：

```text
空闲
  │
  ├─ 读请求 → 发送 → 空闲
  │
  └─ 写请求
       │
       ├─ UART发送失败
       │    → pending_response
       │    → 投递TestTask
       │    → 空闲
       │
       └─ UART发送成功
            → pending_request
               │
               ├─ FPGA成功
               ├─ FPGA拒绝
               └─ 超时
                    ↓
              pending_response
                    ↓
              投递TestTask
                    ↓
                  空闲
```

## 推荐修改顺序

1. 在 `fpga_comm.h` 定义 `FPGA_REQUEST_ID_NONE`；
2. 在 `communicate_task.c` 增加 `pending_response`；
3. 拆出接收处理、超时检查、响应投递、请求执行四个函数；
4. 修改主循环的等待时间；
5. 修改 `communicate_submit_request()` 返回语义；
6. 在 `test_task.c` 清除 pending ID 并补 `break`；
7. 编译；
8. 分别验证：
    - 写成功；
    - FPGA 返回失败；
    - UART 发送失败；
    - FPGA 不应答；
    - TestTask 队列暂时已满；
    - 连续提交两个写请求；
    - 写请求后队列里还有温度读取请求。

这套修改能解决 MCU 侧可解决的事务生命周期问题。 **迟到 ACK 的绝对可靠识别仍然需要 FPGA 协议回显事务号；仅修改
`communicate_task.c` 无法从根本上解决这一点。**