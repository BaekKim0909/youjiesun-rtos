# STM32F429 按键扫描与 TIM7 中断逻辑说明

## 1. 适用范围

本文对应当前工程的以下硬件和软件配置：

- MCU：STM32F429IGTx
- 按键矩阵：4行×5列，共20个物理按键
- 物理按键编号：按行优先编号1～20
- 定时器：TIM7
- TIM7周期：5ms
- LVGL版本：9.3.0
- 系统结构：无FreeRTOS，LVGL由主循环调用 `lv_timer_handler()`

本次改动的核心目标是：按键按住不释放时，程序不能因为等待释放而卡死；同时保证LVGL能够正确收到按下、保持和释放状态。

## 2. 为什么不能继续使用阻塞式扫描

旧实现中存在两类阻塞：

```c
HAL_Delay(10);
HAL_Delay(20);
while (按键没有释放)
{
}
```

这会造成：

1. 每次扫描耗时几十毫秒；
2. 按键一直按住时，函数永远不返回；
3. 主循环中的LVGL、USB和其他业务无法继续运行；
4. 按键回调收到的按下时刻实际上已经接近释放时刻；
5. 长按和LVGL长按重复机制无法正常工作。

新实现将扫描、消抖和业务处理分离：

```text
TIM7中断：扫描GPIO、消抖、更新按键状态
LVGL回调：读取稳定状态、执行一次性业务、向LVGL报告状态
```

TIM7中断绝不调用LVGL函数、页面函数、串口发送函数或停止测量函数。

## 3. 文件职责

### 3.1 `Core/Inc/key.h`

定义按键驱动的公共接口和输入快照结构：

```c
typedef struct
{
    uint8_t key;
    uint8_t pressed;
    uint32_t press_id;
} key_input_t;
```

字段含义：

- `key`：当前稳定按键编号，范围1～20；
- `pressed`：1表示稳定按下，0表示没有稳定按键；
- `press_id`：每次确认新的按下沿时递增，用于识别一次新的按压。

接口含义：

| 接口 | 调用位置 | 作用 |
|---|---|---|
| `key_init()` | 初始化阶段 | 清空状态，所有行设置为高电平 |
| `key_timer_process()` | TIM7中断 | 执行一轮矩阵扫描和消抖 |
| `key_get_input()` | LVGL输入回调 | 读取非阻塞的稳定输入快照 |
| `key_scan()` | 旧轮询代码 | 读取一次性完整短按事件 |

### 3.2 `Core/Src/key.c`

实现矩阵扫描、上电释放检测、多键锁定、消抖和短按回放。

### 3.3 `Core/Src/stm32f4xx_it.c`

TIM7中断通过HAL回调进入按键驱动：

```c
else if (htim->Instance == TIM7)
{
    key_timer_process();
}
```

TIM7中断只更新按键底层状态，不执行应用层逻辑。

### 3.4 `Core/Src/main.c`

LVGL输入初始化完成后初始化并启动TIM7：

```c
key_init();
HAL_TIM_Base_Start_IT(&htim7);
```

这样可以避免LVGL输入对象创建之前积累无意义的按键事件。

### 3.5 `lv_port_indev_template.c`

负责：

- 从按键驱动读取快照；
- 将物理编号映射为LVGL键值；
- 执行停止测量、停止校准和返回导航等一次性业务；
- 向LVGL报告 `PRESSED` 或 `RELEASED`。

## 4. TIM7扫描周期和消抖

TIM7每5ms调用一次 `key_timer_process()`。

```text
一次采样间隔：5ms
连续一致次数：4次
消抖时间：约20ms
```

按下和释放使用相同的4次确认规则：

```text
原始状态连续4次相同
        ↓
更新稳定状态
```

因此，机械按键弹跳不会直接产生多次按键事件。

## 5. 完整矩阵扫描过程

每次TIM7触发时，驱动扫描全部4行：

1. 所有行输出高电平；
2. 当前行输出低电平；
3. 读取5个列输入；
4. 统计本轮检测到的闭合位置数量；
5. 扫描下一行；
6. 扫描结束后所有行恢复高电平。

由于列为上拉输入，当前行按下的按键会将对应列拉低。

按键编号计算方式：

```c
key = row * 5 + col + 1;
```

例如：

| 行 | 列 | 编号 |
|---:|---:|---:|
| 0 | 0 | 1 |
| 0 | 4 | 5 |
| 1 | 0 | 6 |
| 3 | 4 | 20 |

## 6. 上电时先等待释放

`key_init()`将 `input_enabled` 设置为0。

初始化后，即使检测到按键保持按下，也不会立即产生有效按键。只有连续4次扫描得到全释放，驱动才会把 `input_enabled` 设为1。

这样可以避免：

- 上电时按住按键导致误确认；
- 设备启动过程中误触发停止、确认或返回；
- GPIO初始化瞬态被当作按键事件。

## 7. 多键锁定策略

如果一轮扫描检测到两个或更多闭合位置，立即进入 `multi_key_locked` 状态：

```text
检测到多键
    ↓
丢弃当前按键
    ↓
锁定输入
    ↓
连续4轮检测到全释放
    ↓
恢复输入
```

这样做的原因是当前矩阵没有按键隔离二极管，多个按键同时按下时可能出现鬼键。

本次选择“不认首键”，而是优先保证不会向界面发送错误按键。

## 8. 长按状态

稳定按下后，`stable_key`保持该键值，`key_get_input()`持续返回：

```text
pressed = 1
key = 当前键值
press_id = 本次按压序号
```

LVGL输入回调据此持续报告：

```c
data->state = LV_INDEV_STATE_PRESSED;
data->key = 映射后的LVGL键值;
```

稳定释放后返回：

```c
data->state = LV_INDEV_STATE_RELEASED;
```

因此LVGL可以正常处理长按和长按重复。

## 9. `press_id`的作用

`press_id`只在稳定确认按下沿时递增：

```text
第一次按下：press_id = 1
释放
第二次按下：press_id = 2
```

LVGL回调保存已经处理的 `handled_press_id`，只有检测到新的序号才执行一次性业务。

这样可以保证：

- 长按期间不会重复执行停止测量；
- 长按期间不会重复执行返回导航；
- 同一物理按压不会因为LVGL多次读取而重复触发业务。

## 10. 业务按键消费到释放

以下业务动作会消费本次按压：

- 停止测量；
- 停止电极校准；
- 停止校准流程；
- 返回导航栏。

消费后设置：

```c
consumed_until_release = true;
```

在物理释放之前，LVGL回调不会把该键继续交给新页面控件。这样可以避免：

```text
按键触发停止
    ↓
页面切换
    ↓
用户仍未松手
    ↓
同一个按键误操作新页面
```

## 11. 主循环被阻塞时的短按回放

当前UI中存在一些 `HAL_Delay(100)` 和串口重试流程。主循环阻塞时，TIM7仍能扫描按键，但LVGL暂时无法读取。

如果按键在主循环恢复前已经完成按下和释放，驱动会保存最后一次短按：

```text
按下确认
    ↓
释放确认
    ↓
保存 replay_key、replay_press_id 和释放时间
```

回放规则：

- 只保留最后一次短按；
- 最多保留1秒；
- 当前仍按住的实体按键优先于旧回放事件；
- 回放事件被读取后清除；
- LVGL先收到一次PRESSED，下一次收到RELEASED。

注意：回放只能避免按键事件丢失，不能让阻塞函数立即停止。停止动作仍需等当前UI函数返回到LVGL主循环后才能执行。

## 12. 物理按键切换时的LVGL顺序

如果LVGL已经收到A键的PRESSED，而底层在下一次读取前检测到A释放、B按下，输入回调不会直接发送PRESSED(B)。

它会先暂存B，并发送：

```text
RELEASED(A)
```

下一次读取再发送：

```text
PRESSED(B)
```

这样可以保证按键事件顺序完整。

## 13. 兼容接口 `key_scan()`

`key_scan()`不再直接返回当前长按状态，而是返回已经完成一次“按下并释放”的最后一个键值，并在读取后清除。

因此：

- 旧的轮询代码不会因长按而反复得到同一键；
- LVGL必须使用 `key_get_input()`，不能使用 `key_scan()`实现长按输入；
- `key_scan()`和LVGL回放状态分别维护，互不破坏实时输入状态。

## 14. 中断安全边界

TIM7中断中允许：

- HAL GPIO读写；
- 简单计数；
- 更新按键状态；
- 调用 `HAL_GetTick()`记录时间。

TIM7中断中禁止：

- `HAL_Delay()`；
- 等待按键释放的死循环；
- `lv_timer_handler()`；
- `lv_obj_*()`接口；
- 页面切换；
- 停止测量或停止校准；
- 串口发送；
- 复杂业务逻辑。

## 15. 验证建议

建议在实机上按以下顺序验证：

1. 上电时按住一个按键，确认不会立即误触发；
2. 短按普通键，确认只产生一次点击；
3. 长按方向键，确认LVGL可以连续处理长按；
4. 长按返回/停止键，确认业务只执行一次；
5. 业务动作触发后不松手，确认新页面不被继续操作；
6. 同时按两个键，确认界面不产生错误输入；
7. 多键释放后，确认连续4轮全释放后才能重新输入；
8. 在UI执行 `HAL_Delay(100)`期间短按，确认恢复后最后一次短按可以回放；
9. 在UI阻塞期间按下停止键，确认阻塞结束后动作能够执行；
10. 先按A再按B，确认LVGL事件顺序为 `PRESSED(A)`、`RELEASED(A)`、`PRESSED(B)`。

## 16. 已知限制

1. 本次没有在TIM7中断中直接执行停止业务，因此UI正在执行阻塞函数时，停止动作不能立即发生。
2. 当前行GPIO仍为推挽输出，多键时仍存在电气争用风险；软件会锁定多键，但不能从硬件层消除瞬间高低电平连接。
3. 当前只保留最后一次阻塞期间短按，不保存完整按键队列。
4. 完整Keil链接编译需要在安装Keil MDK且可用 `UV4.exe` 的环境中执行；源码级语法检查不能替代最终链接验证。
