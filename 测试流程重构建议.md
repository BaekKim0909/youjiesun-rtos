# YouJieSun 测试流程检查与重构建议

## 1. 文档目的

本文基于当前 `YouJieSun-Rel` 工程中的主测试流程、电极校准流程、交流校准流程、USART2 接收流程和 LVGL 页面控制逻辑进行整理，目标是：

1. 指出当前需要修改的内容；
2. 说明为什么需要修改；
3. 给出渐进式修改建议和参考代码。

本文仅作为设计与实施参考，代码示例用于说明模块职责和接口，不建议一次性复制后整体替换现有流程。推荐按优先级逐步迁移，并在每一步完成后进行目标板验证。

当前项目环境：

- MCU：STM32F429IGT6；
- LVGL：9.3.0；
- 主要业务代码：C；
- 主测试流程主要位于 `MDK-ARM/Middlewares/LVGL/GUI_APP/youjiesunUI.c`；
- 新交流校准流程主要位于 `MDK-ARM/Middlewares/LVGL/GUI_APP/calibrationPage.c`；
- 协议发送主要位于 `Core/Src/CommunicationProtocal.c`；
- USART2 接收解析主要位于 `Core/Src/stm32f4xx_it.c`。

---

## 2. 当前测试流程的总体理解

当前主测试流程大致如下：

```text
开始测试
  → 检查盖子、油杯、排油状态
  → 写入测试参数
  → 升温
  → 交流测试（按模板可跳过）
  → 放电
  → 正向体积电阻率测试（按参数可跳过）
  → 放电
  → 负向体积电阻率测试（按参数可跳过）
  → 最终放电
  → 读取结果
  → 显示单次结果或进入第二次填充
  → 显示最终结果
```

当前流程不是由一个状态变量完整表示，而是由多组变量共同决定：

- `SystemState`：系统空闲、测试、校准；
- `CurrentPage`：当前页面；
- `CurrentStep`：当前测试步骤；
- `CurrentFillNum`：第一次或第二次填充；
- `CurrentRhoParmStep`：正向或负向体积电阻率阶段；
- `WriteState`：写寄存器应答状态；
- `CurrentState`：下位机动作状态；
- `TimeRegValue`：测试或放电剩余时间；
- `MeasureTimer`、`ReleaseVoltageTimer` 及若干局部定时器。

因此，一个实际业务状态通常需要通过多个变量组合推断。例如，“正在等待负向直流测试完成”可能同时依赖页面、步骤、正负方向、定时器回调和 `CurrentState`。

这种实现可以运行，但随着测试模板、校准项目和异常路径增加，容易出现非法状态组合、迟到应答被误消费、定时器遗留以及页面状态与硬件状态不一致等问题。

---

# 3. 需要优先修改的确定性逻辑问题

## 3.1 状态观察者在正常状态更新时也可能停止测试

### 需要修改的内容

检查以下三个状态观察者：

- `Lid_Observer_cb`；
- `PourState_Observer_cb`；
- `OilCupState_Observer_cb`。

当前逻辑在读取 Subject 的新值之前，就根据 `SystemState` 停止当前测试或校准流程。这样即使收到的是正常状态，也可能触发停止：

- 盖子闭合：`value == 0`；
- 未排油：`value == 0`；
- 油杯正常：`value == 0`。

### 修改原因

观察者应该根据“新状态是否异常”决定是否触发安全停止，而不是仅根据“当前正在测试”就停止。

当前行为等价于：

```text
收到状态更新 → 停止当前流程
```

预期行为应该是：

```text
收到状态更新
  → 更新显示
  → 判断新值是否异常
  → 只有异常时才停止当前流程
```

这是确定性业务逻辑问题，优先级高于架构重构。

### 修改建议

先读取 Subject 值，再更新缓存和 UI，最后仅在异常条件成立时请求停止。

参考代码：

```c
static void handle_safety_fault(test_stop_reason_t reason)
{
    switch (SystemState)
    {
    case MEASURE_ING:
        StopMeasure();
        break;

    case ELECTORED_CAL_ING:
        StopElectrodeCalibration();
        break;

    case CALIBRATION_ING:
        StopCalibrationFlow();
        break;

    case SYSTEM_DEFAULT:
    default:
        break;
    }
}

void Lid_Observer_cb(lv_observer_t *observer, lv_subject_t *subject)
{
    uint16_t value = (uint16_t)lv_subject_get_int(subject);
    LidState = value;

    /* 先更新盖子图标 */
    update_lid_status_view(value);

    /* 只有盖子打开时才停止当前流程 */
    if (value == 1U)
    {
        handle_safety_fault(TEST_STOP_REASON_LID_OPEN);
    }
}
```

排油和油杯观察者采用相同结构：

```c
if (PourState == 1U)
{
    handle_safety_fault(TEST_STOP_REASON_POURING);
}

if (OilCupState == 1U)
{
    handle_safety_fault(TEST_STOP_REASON_OIL_CUP_ABNORMAL);
}
```

进一步建议：观察者最终只发布安全事件，不直接操作测试页面和定时器，由测试流程控制器决定如何停止。

---

## 3.2 从 UART 中断回调直接调用 LVGL 函数

### 需要修改的内容

检查 `HAL_UARTEx_RxEventCallback()` 中对 `lv_async_call()` 的直接调用。

当前 USART2 接收回调在解析盖子、排油、油杯状态后，直接调用 `lv_async_call()` 安排 UI 更新。

### 修改原因

`lv_async_call()` 的含义是“稍后在 LVGL Handler 中执行回调”，但这不代表调用 `lv_async_call()` 本身天然具备 ISR 安全性。

它可能修改 LVGL 内部的异步任务或定时器链表。如果当前 LVGL Port 没有实现 ISR 安全锁，可能出现：

- 偶发 HardFault；
- LVGL 链表损坏；
- 高频串口通信时出现随机异常；
- UI 对象或定时器状态异常。

此项需要结合当前 LVGL Port 的锁实现确认，但应按高风险处理。

### 修改建议

USART ISR 只负责：

1. 接收和校验数据；
2. 解析为不可变事件；
3. 写入固定长度事件队列；
4. 退出中断。

LVGL 线程或主循环再消费事件并更新 Subject/UI。

不依赖 FreeRTOS 的固定环形队列示例：

```c
#define PROTOCOL_EVENT_QUEUE_SIZE 16U

typedef enum
{
    PROTOCOL_EVENT_NONE = 0,
    PROTOCOL_EVENT_WRITE_ACK,
    PROTOCOL_EVENT_TEST_STATE,
    PROTOCOL_EVENT_TEST_TIME,
    PROTOCOL_EVENT_TEMPERATURE,
    PROTOCOL_EVENT_LID_STATE,
    PROTOCOL_EVENT_POUR_STATE,
    PROTOCOL_EVENT_OIL_CUP_STATE,
    PROTOCOL_EVENT_ERROR_CODE,
    PROTOCOL_EVENT_RESULT_READY
} protocol_event_type_t;

typedef struct
{
    protocol_event_type_t type;
    uint16_t register_address;
    uint16_t value_u16;
    uint32_t value_u32;
} protocol_event_t;

static volatile uint8_t event_write_index = 0U;
static volatile uint8_t event_read_index = 0U;
static protocol_event_t event_queue[PROTOCOL_EVENT_QUEUE_SIZE];

bool protocol_event_push_from_isr(const protocol_event_t *event)
{
    uint8_t next = (uint8_t)((event_write_index + 1U) % PROTOCOL_EVENT_QUEUE_SIZE);

    if (next == event_read_index)
    {
        /* 队列已满，记录错误计数，不能覆盖关键事件 */
        return false;
    }

    event_queue[event_write_index] = *event;
    event_write_index = next;
    return true;
}

bool protocol_event_pop(protocol_event_t *event)
{
    if (event_read_index == event_write_index)
    {
        return false;
    }

    *event = event_queue[event_read_index];
    event_read_index =
        (uint8_t)((event_read_index + 1U) % PROTOCOL_EVENT_QUEUE_SIZE);
    return true;
}
```

UART 回调中只发布事件：

```c
protocol_event_t event = {
    .type = PROTOCOL_EVENT_LID_STATE,
    .register_address = LID_STATE_R_REG,
    .value_u16 = sub_val,
    .value_u32 = 0U
};

(void)protocol_event_push_from_isr(&event);
```

在 LVGL 线程周期回调中消费：

```c
void ProtocolEventProcessTimer_cb(lv_timer_t *timer)
{
    protocol_event_t event;

    while (protocol_event_pop(&event))
    {
        switch (event.type)
        {
        case PROTOCOL_EVENT_LID_STATE:
            lv_subject_set_int(&LidSubject, event.value_u16);
            break;

        case PROTOCOL_EVENT_POUR_STATE:
            lv_subject_set_int(&PourSubject, event.value_u16);
            break;

        case PROTOCOL_EVENT_OIL_CUP_STATE:
            lv_subject_set_int(&OilCupSubject, event.value_u16);
            break;

        default:
            test_flow_handle_protocol_event(&event);
            break;
        }
    }
}
```

注意：环形队列在 ISR 与普通上下文并发访问时，需要根据编译器、内存模型和中断优先级确认原子性。若项目正式使用 FreeRTOS，可改用支持 FromISR 的队列接口。

---

# 4. 将隐式测试流程改为显式事件驱动状态机

## 4.1 需要修改的内容

当前主测试状态由 `SystemState`、`CurrentPage`、`CurrentStep`、`CurrentFillNum`、`CurrentRhoParmStep` 和多个定时器共同决定。

建议建立一个深模块 `TestFlowController`，由它唯一负责：

- 测试阶段转换；
- 命令发送时机；
- 填充轮次；
- 正负极性选择；
- 定时器所有权；
- 正常完成；
- 用户停止；
- 安全故障停止；
- 超时和通信错误。

### 修改原因

多个正交全局变量会产生以下问题：

1. 可能形成不合法组合；
2. 阅读代码时需要跨多个回调推断状态；
3. 页面被销毁不一定等于硬件已停止；
4. 某个迟到应答可能推进错误阶段；
5. 正常完成和异常停止的资源清理不一致。

### 修改建议

第一步不需要引入第三方状态机框架，只需要：

- 一个状态枚举；
- 一个上下文结构体；
- 一个事件枚举；
- 一个事件处理函数；
- 一个统一完成函数；
- 一个统一中止函数。

### 状态定义示例

```c
typedef enum
{
    TEST_FLOW_IDLE = 0,
    TEST_FLOW_WRITING_PARAMETERS,
    TEST_FLOW_STARTING_HEAT,
    TEST_FLOW_WAITING_TEMPERATURE,
    TEST_FLOW_WAITING_HEAT_DONE,
    TEST_FLOW_STARTING_AC,
    TEST_FLOW_AC_RUNNING,
    TEST_FLOW_DISCHARGING_BEFORE_DC_POS,
    TEST_FLOW_STARTING_DC_POS,
    TEST_FLOW_DC_POS_RUNNING,
    TEST_FLOW_DISCHARGING_BEFORE_DC_NEG,
    TEST_FLOW_STARTING_DC_NEG,
    TEST_FLOW_DC_NEG_RUNNING,
    TEST_FLOW_FINAL_DISCHARGE,
    TEST_FLOW_READING_RESULT,
    TEST_FLOW_WAITING_SECOND_FILL,
    TEST_FLOW_STOPPING,
    TEST_FLOW_COMPLETED,
    TEST_FLOW_FAILED
} test_flow_state_t;
```

### 事件定义示例

```c
typedef enum
{
    TEST_EVENT_NONE = 0,
    TEST_EVENT_START_REQUESTED,
    TEST_EVENT_WRITE_ACK_SUCCESS,
    TEST_EVENT_WRITE_ACK_FAILED,
    TEST_EVENT_TEMPERATURE_RECEIVED,
    TEST_EVENT_TIME_RECEIVED,
    TEST_EVENT_DEVICE_ACTION_DONE,
    TEST_EVENT_RESULT_RECEIVED,
    TEST_EVENT_SECOND_FILL_CONFIRMED,
    TEST_EVENT_STOP_REQUESTED,
    TEST_EVENT_SAFETY_FAULT,
    TEST_EVENT_TIMEOUT
} test_event_type_t;

typedef struct
{
    test_event_type_t type;
    union
    {
        float temperature;
        uint16_t time_seconds;
        uint16_t device_state;
        test_stop_reason_t stop_reason;
    } data;
} test_event_t;
```

### 上下文结构示例

```c
typedef struct
{
    test_flow_state_t state;
    test_plan_t plan;

    uint8_t current_fill;
    uint8_t retry_count;
    uint32_t generation;
    uint32_t deadline_tick;

    bool stop_requested;
    bool transaction_pending;

    lv_timer_t *poll_timer;
    lv_timer_t *timeout_timer;
} test_flow_context_t;

static test_flow_context_t test_flow;
```

`generation` 用于区分不同测试批次。每次启动测试时递增，异步事件带上对应代次后，旧测试迟到事件可以被丢弃。

### 小型外部接口示例

```c
bool test_flow_start(const test_request_t *request);
void test_flow_request_stop(test_stop_reason_t reason);
void test_flow_handle_event(const test_event_t *event);
test_flow_state_t test_flow_get_state(void);
```

调用者不应该直接操作 `MeasureTimer`、`ReleaseVoltageTimer`、`CurrentStep` 或清零 `CurrentState`。

### 状态处理示例

```c
void test_flow_handle_event(const test_event_t *event)
{
    if (event == NULL)
    {
        return;
    }

    if (event->type == TEST_EVENT_STOP_REQUESTED ||
        event->type == TEST_EVENT_SAFETY_FAULT)
    {
        test_flow_abort(event->data.stop_reason);
        return;
    }

    switch (test_flow.state)
    {
    case TEST_FLOW_WRITING_PARAMETERS:
        if (event->type == TEST_EVENT_WRITE_ACK_SUCCESS)
        {
            test_flow_enter_starting_heat();
        }
        else if (event->type == TEST_EVENT_WRITE_ACK_FAILED ||
                 event->type == TEST_EVENT_TIMEOUT)
        {
            test_flow_abort(TEST_STOP_REASON_COMMUNICATION_ERROR);
        }
        break;

    case TEST_FLOW_WAITING_TEMPERATURE:
        if (event->type == TEST_EVENT_TEMPERATURE_RECEIVED)
        {
            test_view_update_temperature(event->data.temperature);

            if (event->data.temperature >= test_flow.plan.target_temperature)
            {
                test_flow.state = TEST_FLOW_WAITING_HEAT_DONE;
                protocol_request_test_state();
            }
        }
        break;

    case TEST_FLOW_WAITING_HEAT_DONE:
        if (event->type == TEST_EVENT_DEVICE_ACTION_DONE)
        {
            if (test_flow.plan.enable_ac)
            {
                test_flow_enter_starting_ac();
            }
            else
            {
                test_flow_enter_discharge_before_dc_pos();
            }
        }
        break;

    case TEST_FLOW_AC_RUNNING:
        if (event->type == TEST_EVENT_DEVICE_ACTION_DONE)
        {
            if (test_flow.plan.enable_dc_positive)
            {
                test_flow_enter_discharge_before_dc_pos();
            }
            else
            {
                test_flow_enter_final_discharge();
            }
        }
        break;

    default:
        break;
    }
}
```

状态转移函数应当统一完成该状态的进入动作：

```c
static void test_flow_enter_starting_ac(void)
{
    test_flow.state = TEST_FLOW_STARTING_AC;
    test_flow.retry_count = 0U;

    test_view_show_ac_page(test_flow.current_fill);
    protocol_submit_write_command(
        TEST_CONTROL_W_REG,
        DIELECTRIC_LOSS_COMMAND,
        TEST_TRANSACTION_START_AC);
}
```

---

# 5. 使用测试计划代替分散的模板判断

## 5.1 需要修改的内容

当前测试模板、填充次数、是否执行 AC、是否执行 Rho+、是否执行 Rho- 等判断分散在多个 Timer 回调和页面处理函数中。

### 修改原因

当增加新模板或调整流程时，必须在多个位置同步修改条件分支，容易遗漏。每个阶段还需要理解完整的模板含义，增加认知负担。

### 修改建议

测试开始时，将用户选择转换成一次不可变的 `test_plan_t`，之后状态机只读取计划，不再重复解释模板。

参考代码：

```c
typedef struct
{
    bool enable_heating;
    bool enable_ac;
    bool enable_dc_positive;
    bool enable_dc_negative;

    uint8_t fill_count;
    int32_t target_temperature;
    uint16_t ac_voltage;
    uint16_t dc_voltage;
    uint16_t frequency;
} test_plan_t;

bool test_plan_build(const MeasureStandard *standard,
                     uint8_t rho_parameter,
                     test_plan_t *plan)
{
    if (standard == NULL || plan == NULL)
    {
        return false;
    }

    memset(plan, 0, sizeof(*plan));

    plan->enable_heating = true;
    plan->enable_ac = (standard->template != NO_AC_TEMPLATE);
    plan->enable_dc_positive = (rho_parameter >= 1U);
    plan->enable_dc_negative = (rho_parameter >= 2U);
    plan->fill_count = standard->fillNum;
    plan->target_temperature = standard->temperature;
    plan->ac_voltage = standard->ACVoltage;
    plan->dc_voltage = standard->DCVoltage;
    plan->frequency = standard->frequency;

    if (plan->fill_count < 1U || plan->fill_count > 2U)
    {
        return false;
    }

    return true;
}
```

状态机中的分支会变得直接：

```c
if (test_flow.plan.enable_ac)
{
    test_flow_enter_starting_ac();
}
else if (test_flow.plan.enable_dc_positive)
{
    test_flow_enter_discharge_before_dc_pos();
}
else
{
    test_flow_enter_final_discharge();
}
```

后续如果流程继续扩大，可以将计划进一步转换为阶段表，但不建议第一步就引入复杂阶段对象。

---

# 6. 将串口命令改为可匹配的异步事务

## 6.1 需要修改的内容

当前多个发送流程使用相同模式：

```text
WriteState = 0
发送命令
HAL_Delay(100)
检查 WriteState
失败则重试，最多 5 次
```

需要逐步替换单个全局 `WriteState` 应答槽，以及 LVGL 回调内的阻塞等待。

### 修改原因

#### 原因一：应答无法与请求匹配

`WriteState == 1` 只能说明“某条写命令成功”，不能说明成功应答对应哪条请求。迟到应答或重叠命令可能被错误流程认领。

#### 原因二：阻塞 LVGL

最多 5 次、每次 `HAL_Delay(100)`，可能阻塞 LVGL 线程约 500ms。期间 UI 刷新、动画、输入、Timer 和异步回调都会延迟。

#### 原因三：取消困难

同步等待期间，用户停止、安全故障和页面退出不能及时接管流程。

### 修改建议

建立单一串口事务模块，负责：

- UART 发送串行化；
- 保存当前事务；
- 根据功能码、地址和数量匹配响应；
- 非阻塞超时；
- 自动重试；
- 向测试状态机投递成功或失败事件。

### 事务类型示例

```c
typedef enum
{
    TEST_TRANSACTION_NONE = 0,
    TEST_TRANSACTION_WRITE_PARAMETERS,
    TEST_TRANSACTION_START_HEAT,
    TEST_TRANSACTION_START_AC,
    TEST_TRANSACTION_START_DC_POS,
    TEST_TRANSACTION_START_DC_NEG,
    TEST_TRANSACTION_START_DISCHARGE,
    TEST_TRANSACTION_STOP,
    TEST_TRANSACTION_EXIT_CALIBRATION
} test_transaction_type_t;

typedef struct
{
    bool active;
    test_transaction_type_t type;
    uint16_t register_address;
    uint16_t register_count;
    uint8_t retry_count;
    uint8_t max_retries;
    uint32_t deadline_tick;
    uint32_t generation;
} protocol_transaction_t;
```

### 提交事务示例

```c
bool protocol_submit_write_command(uint16_t register_address,
                                   uint16_t value,
                                   test_transaction_type_t type)
{
    if (current_transaction.active)
    {
        return false;
    }

    current_transaction.active = true;
    current_transaction.type = type;
    current_transaction.register_address = register_address;
    current_transaction.register_count = 1U;
    current_transaction.retry_count = 0U;
    current_transaction.max_retries = 5U;
    current_transaction.deadline_tick = HAL_GetTick() + 100U;
    current_transaction.generation = test_flow_get_generation();

    semd_command_write_reg(register_address, value);
    return true;
}
```

### 响应匹配示例

```c
void protocol_transaction_on_response(const protocol_response_t *response)
{
    test_event_t event;

    if (response == NULL || !current_transaction.active)
    {
        return;
    }

    if (response->function_code != 0x06U ||
        response->register_address != current_transaction.register_address ||
        response->register_count != current_transaction.register_count)
    {
        /* 不属于当前事务，交给其他响应分发逻辑 */
        return;
    }

    current_transaction.active = false;

    event.type = response->write_state == 1U
                     ? TEST_EVENT_WRITE_ACK_SUCCESS
                     : TEST_EVENT_WRITE_ACK_FAILED;

    test_flow_handle_event(&event);
}
```

### 非阻塞超时和重试示例

```c
void protocol_transaction_tick(uint32_t now)
{
    test_event_t event;

    if (!current_transaction.active)
    {
        return;
    }

    if ((int32_t)(now - current_transaction.deadline_tick) < 0)
    {
        return;
    }

    if (current_transaction.retry_count <
        current_transaction.max_retries)
    {
        current_transaction.retry_count++;
        current_transaction.deadline_tick = now + 100U;
        protocol_resend_current_transaction();
        return;
    }

    current_transaction.active = false;
    event.type = TEST_EVENT_TIMEOUT;
    test_flow_handle_event(&event);
}
```

在该结构稳定后，逐步淘汰业务代码对 `WriteState` 的直接读取。

---

# 7. CurrentState 和 TimeRegValue 需要带响应语义

## 7.1 需要修改的内容

当前 `CurrentState` 和 `TimeRegValue` 是跨阶段共享缓存。多个流程会直接读取、清零，并等待 ISR 再次写入。

### 修改原因

可能发生以下时序：

```text
阶段 A 收到完成状态 2
阶段 A 清零 CurrentState
阶段 A 的重复或迟到响应再次写入 2
阶段 B 刚启动便读取到 2
阶段 B 被误判为已完成
```

`TimeRegValue` 也可能在新阶段开始后短暂保留上个阶段的旧值。

### 修改建议

短期方案：

- 每次发起读取时记录请求代次和期望阶段；
- 响应到达后，只向当前阶段投递事件；
- 状态机消费事件，不直接轮询共享缓存。

响应示例：

```c
typedef struct
{
    uint8_t function_code;
    uint16_t register_address;
    uint16_t register_count;
    uint16_t value_u16;
    uint32_t generation;
} protocol_response_t;
```

状态事件需要携带代次：

```c
typedef struct
{
    test_event_type_t type;
    uint32_t generation;
    uint16_t register_address;
    uint16_t value;
} test_protocol_event_t;
```

消费时拒绝旧事件：

```c
if (event->generation != test_flow.generation)
{
    /* 丢弃上一轮测试的迟到响应 */
    return;
}
```

如果下位机协议不能提供事务序号，至少可以通过以下信息匹配：

- 当前仅允许一个待处理事务；
- 功能码；
- 寄存器地址；
- 寄存器数量；
- 当前状态机阶段；
- 本地测试代次。

---

# 8. 统一定时器所有权和退出路径

## 8.1 需要修改的内容

当前 `MeasureTimer`、`ReleaseVoltageTimer` 和校准局部 Timer 的创建、暂停、修改回调和删除分散在多个函数中。

需要让一个流程控制模块成为这些定时器的唯一所有者。

### 修改原因

定时器所有权分散容易导致：

- 页面销毁后 Timer 仍然回调；
- Timer 已删除但指针未清空；
- 正常完成删除了部分资源，异常停止删除了另一部分；
- 只停止 UI 轮询，但下位机仍在运行；
- 多个停止入口重复发送停止命令；
- 旧电极校准局部 Timer 无法被停止函数立即删除。

### 修改建议

所有流程 Timer 都保存在上下文中，只能由控制模块创建和删除。

```c
static void test_flow_delete_timer(lv_timer_t **timer)
{
    if (timer == NULL || *timer == NULL)
    {
        return;
    }

    lv_timer_delete(*timer);
    *timer = NULL;
}

static void test_flow_cleanup_timers(void)
{
    test_flow_delete_timer(&test_flow.poll_timer);
    test_flow_delete_timer(&test_flow.timeout_timer);
}
```

正常完成统一进入 `test_flow_finish()`：

```c
static void test_flow_finish(void)
{
    if (test_flow.state == TEST_FLOW_COMPLETED ||
        test_flow.state == TEST_FLOW_IDLE)
    {
        return;
    }

    test_flow.state = TEST_FLOW_COMPLETED;

    test_flow_cleanup_timers();
    protocol_cancel_current_transaction(test_flow.generation);

    SystemState = SYSTEM_DEFAULT;
    CurrentState = 0U;
    TimeRegValue = 0U;

    test_view_show_result();

    test_flow.state = TEST_FLOW_IDLE;
}
```

异常和用户停止统一进入 `test_flow_abort()`：

```c
static void test_flow_abort(test_stop_reason_t reason)
{
    if (test_flow.state == TEST_FLOW_IDLE ||
        test_flow.state == TEST_FLOW_STOPPING)
    {
        return;
    }

    test_flow.state = TEST_FLOW_STOPPING;
    test_flow.stop_requested = true;

    test_flow_cleanup_timers();
    protocol_cancel_current_transaction(test_flow.generation);

    /* 停止命令由一个位置发送，避免重复停止 */
    protocol_submit_write_command(
        TEST_CONTROL_W_REG,
        STOP_MEASURE_COMMAND,
        TEST_TRANSACTION_STOP);

    SystemState = SYSTEM_DEFAULT;
    CurrentState = 0U;
    TimeRegValue = 0U;

    test_view_show_abort_reason(reason);
    test_flow.state = TEST_FLOW_IDLE;
}
```

如果停止命令也必须等待应答，则 `test_flow_abort()` 不应立即进入 `IDLE`，而应停留在 `TEST_FLOW_STOPPING`，收到停止应答或停止超时后再完成清理。

---

# 9. 页面和业务状态需要解耦

## 9.1 需要修改的内容

当前部分 Timer 回调通过 `CurrentPage` 判断业务流程是否继续，导航或页面销毁时也可能直接删除测试 Timer。

### 修改原因

页面状态不等于硬件状态：

- 页面切换不一定表示测试已停止；
- 弹窗覆盖页面时测试仍可能继续；
- 页面被意外重建后，下位机可能仍在运行；
- 只删除 Timer 会让 UI 停止轮询，但硬件继续执行。

### 修改建议

页面只负责显示，不拥有测试流程。

推荐关系：

```text
用户点击开始
  → test_flow_start()
  → 流程控制器推进状态
  → 流程控制器通知页面渲染

用户返回
  → test_flow_request_stop(USER_CANCEL)
  → 停止完成后再切换页面
```

页面渲染接口示例：

```c
typedef struct
{
    void (*show_heating)(int32_t target_temperature);
    void (*update_temperature)(float temperature);
    void (*show_ac_running)(uint8_t fill_index);
    void (*show_dc_running)(bool negative);
    void (*update_time)(uint16_t seconds);
    void (*show_result)(void);
    void (*show_error)(test_stop_reason_t reason);
} test_view_adapter_t;
```

如果当前只有一套真实页面，不必为了“可能替换”建立复杂 Adapter 层级。第一步可以先把页面函数集中成一组私有渲染函数，等需要 PC 测试或第二种界面时再正式建立 Adapter seam。

---

# 10. 新交流校准流程的收尾一致性

## 10.1 正常完成路径与停止路径不对称

### 需要修改的内容

检查 `FinishAcCalibrationFlow()` 与异步停止路径的硬件收尾动作是否一致。

当前正常完成主要执行：

- 删除校准 Timer；
- 清理 `CurrentState`；
- 恢复 `SystemState`；
- 删除 Overlay；
- 恢复焦点。

停止路径还会：

- 发送停止测试命令；
- 写入退出校准动作值，例如 `0x00FF`。

### 修改原因

如果下位机不会在校准完成后自动退出校准模式，正常完成路径可能在硬件端遗留校准状态。

这需要根据下位机协议确认，不能仅通过 UI 行为判断。

### 修改建议

先明确协议不变量：

```text
无论正常完成还是异常停止，是否都必须发送退出校准动作命令？
```

如果答案是“必须”，将公共硬件收尾集中到一个函数：

```c
static void ExitAcCalibrationHardwareMode(bool stop_measure)
{
    if (stop_measure)
    {
        SendStopMeasureCommand();
    }

    semd_command_write_reg(CAL_CAL_ACT_W_REG, 0x00FFU);
}
```

正常完成：

```c
static void FinishAcCalibrationFlow(void)
{
    if (ac_cal_flow_state == AC_CAL_FLOW_FINISHING ||
        ac_cal_flow_state == AC_CAL_FLOW_IDLE)
    {
        return;
    }

    ac_cal_flow_state = AC_CAL_FLOW_FINISHING;

    DeleteCalibrationTimer();
    ExitAcCalibrationHardwareMode(false);
    RestoreCalibrationUi();

    CurrentState = 0U;
    SystemState = SYSTEM_DEFAULT;
    ac_cal_flow_state = AC_CAL_FLOW_IDLE;
}
```

异常停止：

```c
static void AbortAcCalibrationFlow(void)
{
    ac_cal_flow_state = AC_CAL_FLOW_FINISHING;

    DeleteCalibrationTimer();
    ExitAcCalibrationHardwareMode(true);
    RestoreCalibrationUi();

    CurrentState = 0U;
    SystemState = SYSTEM_DEFAULT;
    ac_cal_flow_state = AC_CAL_FLOW_IDLE;
}
```

如果下位机自动退出，则应在代码注释或协议文档中明确说明，避免维护者误加重复命令。

---

## 10.2 拟合参数先修改本地数据，后写入设备

### 需要修改的内容

当前普通 AC 拟合流程中，用户输入值可能先写入本地数组和表格，然后才写入下位机。若设备写入失败，本地 UI 和设备状态会不一致。

### 修改原因

当前提交顺序可能产生：

```text
UI：新值
内存模型：新值
下位机：旧值
```

对于仪器参数，界面通常应显示设备已经确认生效的值，而不是仅显示用户期望值。

### 修改建议

推荐使用“确认后提交”：

```c
static void HandleAcFitConfirm_cb(lv_event_t *event)
{
    uint32_t pending_value;

    if (!parse_ac_fit_input(event, &pending_value))
    {
        show_notice_message_box((notice_message_t){
            .result_type = NOTICE_ERROR,
            .message = "Value Invalid"
        });
        return;
    }

    if (!send_ac_fit_value_with_ack(pending_value))
    {
        show_notice_message_box((notice_message_t){
            .result_type = NOTICE_ERROR,
            .message = "Write Value Failed"
        });
        return;
    }

    /* 设备确认成功后再提交本地模型和UI */
    ac_voltage_items[selected_index].standard_value = pending_value;
    UpdateAcCalibrationTableRow(selected_index);

    show_notice_message_box((notice_message_t){
        .result_type = NOTICE_SUCCESS,
        .message = "Write Success"
    });
}
```

更推荐的最终形态是非阻塞事务：先保存 `pending_value`，收到写成功事件后再更新模型和表格。

```c
typedef struct
{
    bool active;
    uint8_t item_index;
    uint32_t pending_value;
} ac_fit_pending_t;

static ac_fit_pending_t ac_fit_pending;
```

收到应答后提交：

```c
static void CommitPendingAcFitValue(void)
{
    if (!ac_fit_pending.active)
    {
        return;
    }

    ac_voltage_items[ac_fit_pending.item_index].standard_value =
        ac_fit_pending.pending_value;

    UpdateAcCalibrationTableRow(ac_fit_pending.item_index);
    ac_fit_pending.active = false;
}
```

---

# 11. 通知模块建议保持纯显示职责

## 11.1 需要修改的内容

`show_notice_message_box()` 可以作为统一通知显示函数，但不应参与测试状态推进、Timer 所有权或硬件停止。

此外，通知结果枚举应避免使用与 CMSIS 重名的 `SUCCESS` 和 `ERROR`。

### 修改原因

CMSIS 已定义 `SUCCESS` 和 `ERROR` 枚举成员。通知模块再次使用这些全局枚举名，业务文件同时包含相关头文件时可能编译冲突。

### 修改建议

使用带模块前缀的枚举：

```c
typedef enum
{
    NOTICE_SUCCESS = 0,
    NOTICE_WARNING,
    NOTICE_ERROR
} notice_result_t;

typedef struct
{
    notice_result_t result_type;
    const char *message;
} notice_message_t;
```

显示函数保持只处理：

- 颜色；
- 文案；
- 尺寸；
- 自动关闭。

调用示例：

```c
show_notice_message_box((notice_message_t){
    .result_type = NOTICE_ERROR,
    .message = "Value Invalid"
});
```

字符串字面量位于只读存储区，不会随局部结构体栈空间释放；`lv_label_set_text()` 会复制文本。若改用 `lv_label_set_text_static()`，则必须重新检查文本生命周期。

---

# 12. 清理头文件中过宽或失效的接口

## 12.1 需要修改的内容

检查以下情况：

1. 头文件有声明但没有对应实现；
2. 已废弃函数仍保留声明；
3. `calibrationPage.h` 中声明大量 `static` 私有函数；
4. 调用方不需要知道的 Timer 回调和内部状态被暴露。

当前检查到的可疑声明包括：

- `SendReadReleaseVoltageRegCommand`；
- `HandleStopMeasureBtn_cb`；
- `SendReadReleaseVoltage_cb`。

修改前应再次全仓搜索，确认它们没有由条件编译或其他目标实现。

### 修改原因

头文件是模块接口。失效声明和私有实现泄漏会：

- 误导调用者；
- 增加维护者认知负担；
- 让模块显得比实际更复杂；
- 产生潜在链接错误；
- 降低模块深度。

### 修改建议

`calibrationPage.h` 只保留外部真正需要调用的入口，例如：

```c
#ifndef CALIBRATION_PAGE_H
#define CALIBRATION_PAGE_H

#include <stdbool.h>
#include <stdint.h>

void CalibrationPage_Create(void);
void CalibrationPage_Destroy(void);

bool CalibrationFlow_Start(uint8_t calibration_item);
void CalibrationFlow_RequestStop(void);
bool CalibrationFlow_IsRunning(void);

#endif
```

Timer 回调、按键内部处理、拟合辅助函数和状态转换函数保留在 `calibrationPage.c`，并声明为 `static`。

---

# 13. 推荐的模块划分

建议逐步形成以下模块，而不是继续把所有流程集中在 `youjiesunUI.c`：

```text
TestFlowController
  ├── 状态机
  ├── 测试计划
  ├── 填充轮次
  ├── 正负极性阶段
  ├── Timer所有权
  └── finish/abort统一清理

ProtocolTransaction
  ├── UART发送串行化
  ├── 当前事务
  ├── 应答匹配
  ├── 重试
  └── 超时

ProtocolEventQueue
  ├── ISR发布事件
  └── LVGL线程消费事件

TestView
  ├── 页面创建和切换
  ├── 温度/时间显示
  ├── 结果显示
  └── 通知显示
```

推荐的外部调用关系：

```text
LVGL按键事件
  → TestFlowController.start/stop

UART ISR
  → ProtocolEventQueue.push

LVGL线程
  → ProtocolEventQueue.pop
  → ProtocolTransaction匹配
  → TestFlowController.handle_event
  → TestView渲染
```

这样可以确保：

- UART ISR 不直接操作 UI；
- 页面不直接拥有硬件测试流程；
- 通知框不控制测试状态；
- 协议层不决定业务下一阶段；
- 测试流程只有一个状态和资源所有者。

---

# 14. 推荐实施顺序

## 第一阶段：先修确定性问题

1. 调整盖子、排油、油杯 Observer 的判断顺序；
2. 确保只有异常值才触发停止；
3. 确认所有停止入口不会重复执行；
4. 确认返回导航时是否需要先停止下位机；
5. 确认正常完成校准是否必须退出硬件校准模式。

这一阶段不需要整体重构。

## 第二阶段：统一资源清理

1. 为主测试建立上下文结构体；
2. 将所有测试 Timer 句柄集中到上下文；
3. 建立统一 `finish()`；
4. 建立统一 `abort()`；
5. 页面销毁不再直接代表测试停止。

## 第三阶段：建立显式状态机

1. 定义主测试状态枚举；
2. 将 Timer 回调转换成事件；
3. 集中处理状态转换；
4. 将 `CurrentStep`、`CurrentRhoParmStep` 逐步收进上下文；
5. 以新交流校准中的显式状态作为迁移参考。

## 第四阶段：去除 HAL_Delay 重试

1. 保留现有协议帧格式；
2. 建立非阻塞 deadline；
3. 超时后重发；
4. 收到匹配应答后推进状态机；
5. 逐步移除业务代码对 `WriteState` 的轮询。

## 第五阶段：ISR 与 LVGL 解耦

1. ISR 只发布事件；
2. LVGL线程消费事件；
3. UI状态事件允许合并最新值；
4. ACK、完成、错误事件必须按序保留；
5. 加入队列满计数和诊断信息。

## 第六阶段：引入测试计划

1. 测试开始时生成 `test_plan_t`；
2. 集中解释模板和 Rho 参数；
3. 删除各阶段对模板类型的重复判断；
4. 视后续复杂度决定是否进一步采用阶段表。

---

# 15. 验证建议

每个阶段修改后，至少验证以下场景。

## 15.1 正常流程

- 完整测试，一次填充；
- 完整测试，两次填充；
- 无 AC 模板；
- 无 DC 模板；
- 只测 Rho+；
- 测 Rho+ 和 Rho-；
- 测试结果读取成功；
- 校准普通模式完成；
- 校准回采模式完成。

## 15.2 通信异常

- 写参数无应答；
- 写参数返回失败；
- ACK 迟到；
- ACK 重复；
- 状态帧重复；
- 状态帧粘包；
- CRC 错误；
- 测试时间响应迟到；
- 上一阶段完成状态在下一阶段到达。

## 15.3 用户操作

- 测试中按停止；
- 放电中按停止；
- 停止与测试完成几乎同时发生；
- 快速连续点击两次开始；
- 弹出通知时切换页面；
- 页面销毁后收到迟到响应。

## 15.4 安全联锁

- 盖子正常状态更新不应停止测试；
- 油杯正常状态更新不应停止测试；
- 未排油状态更新不应停止测试；
- 测试中盖子打开必须停止；
- 测试中油杯异常必须停止；
- 测试中进入排油状态必须停止；
- 同一个异常重复上报不能重复创建停止流程。

## 15.5 资源检查

- 每次流程结束后所有 Timer 均为空；
- 停止后下位机确实退出测试；
- 校准完成后下位机确实退出校准模式；
- 没有旧 Timer 在新页面回调；
- 没有旧测试响应推进新测试；
- 长时间重复启动和停止不出现 LVGL 内存增长。

---

# 16. 最终建议

当前代码最适合的核心设计不是堆叠多个复杂设计模式，而是建立一个小接口、深实现的测试流程模块：

```c
bool test_flow_start(const test_request_t *request);
void test_flow_request_stop(test_stop_reason_t reason);
void test_flow_handle_event(const test_event_t *event);
test_flow_state_t test_flow_get_state(void);
```

模块内部统一隐藏：

- 测试状态；
- 测试计划；
- Timer；
- 命令事务；
- 重试；
- 超时；
- 填充轮次；
- 正负极性；
- 正常完成；
- 异常停止；
- 页面渲染时机。

建议最终采用以下组合：

1. **事件驱动有限状态机**：表达测试阶段和合法转移；
2. **命令事务模式**：处理发送、应答匹配、重试和超时；
3. **测试计划策略**：集中表达不同测试模板；
4. **事件队列**：隔离 UART ISR 与 LVGL/业务层；
5. **单一资源所有者**：统一管理 Timer 和退出路径。

其中最优先的不是大规模重构，而是：

1. 修复正常状态更新也会停止测试的问题；
2. 确认 ISR 中调用 LVGL 的安全性；
3. 统一 Timer 和停止资源的所有权；
4. 用显式状态机逐步替代页面、Timer 和多个全局变量共同编码流程；
5. 最后再用异步事务替代 `HAL_Delay + WriteState`。

这样能够在不一次性推翻现有代码的前提下，提高测试流程的可读性、可维护性、实时性和异步正确性。
